load("//toolchain/profile:BUILD.bzl", "cc_toolchain_profile")
load(":BUILD.bzl", "fix_oc_cflags")

## local profile generated by ./configure

OC_CFLAGS = fix_oc_cflags("-O2 -fno-strict-aliasing -fwrapv -pthread -g -Wall -Werror -fno-common".split(" "))

#####################
cc_toolchain_profile(
    name     = "local",
    CFLAGS   = OC_CFLAGS,
    CPPFLAGS = " -D_FILE_OFFSET_BITS=64 ".split(" "),
    LDFLAGS  = "".split(" "),
    # DEFINES  = []
)
