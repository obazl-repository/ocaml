exports_files(["BUILD.bazel"])

## toolchains where buildhost == bootstrap

load("//toolchain/bootstrap:adapter.bzl", "bootstrap_toolchain_adapter")

######## toolchain adapters ########

bootstrap_toolchain_adapter(
    name     = "macos",
    linkmode = "dynamic"
)

