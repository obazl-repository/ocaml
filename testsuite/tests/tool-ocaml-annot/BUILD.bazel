

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-ocaml-annot_lib",
    tests  = [
        ":Failure",
        ":Success",
        ":Typeonly",
    ],
)

ocaml_module(
    name   = "Failure",
    struct = "failure.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Success",
    struct = "success.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Typeonly",
    struct = "typeonly.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

