## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_tool-ocaml-annot_lib",
    modules  = [
        ":Failure",
        ":Success",
        ":Typeonly",
    ],
)

ocaml_module(
    name   = "Failure",
    struct = "failure.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Success",
    struct = "success.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Typeonly",
    struct = "typeonly.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

