## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-unix_unix-execvpe_lib",
    manifest  = [
        ":Exec",
    ],
)

ocaml_module(
    name   = "Exec",
    struct = "exec.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

