

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-unix_isatty_lib",
    tests  = [
        ":Isatty_std",
        ":Isatty_tty",
    ],
)

ocaml_module(
    name   = "Isatty_std",
    struct = "isatty_std.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Isatty_tty",
    struct = "isatty_tty.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

