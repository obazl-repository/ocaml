## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-unix_kill_lib",
    manifest  = [
        ":Unix_kill",
    ],
)

ocaml_module(
    name   = "Unix_kill",
    struct = "unix_kill.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

