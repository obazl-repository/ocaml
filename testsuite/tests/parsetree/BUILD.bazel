

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_parsetree_lib",
    tests  = [
        ":Locations_test",
        ":Source",
        ":Test",
    ],
)

ocaml_module(
    name   = "Locations_test",
    struct = "locations_test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Source",
    struct = "source.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

