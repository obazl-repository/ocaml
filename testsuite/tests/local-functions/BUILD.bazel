

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_local-functions_lib",
    tests  = [
        ":Tupled",
        ":Tupled2",
    ],
)

ocaml_module(
    name   = "Tupled",
    struct = "tupled.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Tupled2",
    struct = "tupled2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

