## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_local-functions_lib",
    manifest  = [
        ":Tupled",
        ":Tupled2",
    ],
)

ocaml_module(
    name   = "Tupled",
    struct = "tupled.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Tupled2",
    struct = "tupled2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

