## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_basic-manyargs_lib",
    manifest  = [
        ":Manyargs",
    ],
)

ocaml_module(
    name   = "Manyargs",
    struct = "manyargs.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

