

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_basic-manyargs_lib",
    tests  = [
        ":Manyargs",
    ],
)

ocaml_module(
    name   = "Manyargs",
    struct = "manyargs.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

