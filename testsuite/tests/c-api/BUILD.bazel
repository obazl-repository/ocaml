

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_c-api_lib",
    tests  = [
        ":Alloc_async",
    ],
)

ocaml_module(
    name   = "Alloc_async",
    struct = "alloc_async.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

