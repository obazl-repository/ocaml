

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-dynlink-bytecode_lib",
    tests  = [
        ":Main",
        ":Plug1",
        ":Plug2",
        ":Registry",
    ],
)

ocaml_module(
    name   = "Main",
    struct = "main.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Plug1",
    struct = "plug1.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Plug2",
    struct = "plug2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Registry",
    struct = "registry.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

