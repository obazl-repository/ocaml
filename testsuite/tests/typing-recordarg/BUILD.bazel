

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-recordarg_lib",
    tests  = [
        ":Recordarg",
    ],
)

ocaml_module(
    name   = "Recordarg",
    struct = "recordarg.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

