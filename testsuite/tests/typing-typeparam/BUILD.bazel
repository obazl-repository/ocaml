

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-typeparam_lib",
    tests  = [
        ":Newtype",
    ],
)

ocaml_module(
    name   = "Newtype",
    struct = "newtype.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

