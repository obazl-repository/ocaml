

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-set_lib",
    tests  = [
        ":Testmap",
        ":Testset",
    ],
)

ocaml_module(
    name   = "Testmap",
    struct = "testmap.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Testset",
    struct = "testset.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

