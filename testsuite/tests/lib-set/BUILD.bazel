## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-set_lib",
    manifest  = [
        ":Testmap",
        ":Testset",
    ],
)

ocaml_module(
    name   = "Testmap",
    struct = "testmap.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Testset",
    struct = "testset.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

