

load("//test:rules.bzl",
     "ocaml_module",
     "ocaml_signature",
)

MODULE_OPTS = []
SIG_OPTS = ["-nostdlib"]

test_suite(
    name = "testsuite_tests_lib-dynlink-private_plugin2b_lib",
    tests  = [
        ":Cow",
    ],
)

ocaml_module(
    name   = "Cow",
    struct = "cow.ml",
    sig    = "Cow_cmi",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_signature(
    name = "Cow_cmi",
    src  = "cow.mli",
    opts = SIG_OPTS,
    deps   = [
    ]
)

