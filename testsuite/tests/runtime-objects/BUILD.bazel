

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_runtime-objects_lib",
    tests  = [
        ":Tests",
    ],
)

ocaml_module(
    name   = "Tests",
    struct = "Tests.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

