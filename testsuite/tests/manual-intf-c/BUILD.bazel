

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_manual-intf-c_lib",
    tests  = [
        ":Curses",
        ":Prog",
    ],
)

ocaml_module(
    name   = "Curses",
    struct = "curses.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Prog",
    struct = "prog.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

