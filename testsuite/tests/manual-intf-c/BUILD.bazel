## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_manual-intf-c_lib",
    modules  = [
        ":Curses",
        ":Prog",
    ],
)

ocaml_module(
    name   = "Curses",
    struct = "curses.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Prog",
    struct = "prog.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

