

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_load_path_test_lib",
    tests  = [
        ":Driver",
        ":Payload",
    ],
)

ocaml_module(
    name   = "Driver",
    struct = "driver.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Payload",
    struct = "payload.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

