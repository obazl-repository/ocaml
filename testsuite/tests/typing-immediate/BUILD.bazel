## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_typing-immediate_lib",
    modules  = [
        ":Immediate",
    ],
)

ocaml_module(
    name   = "Immediate",
    struct = "immediate.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

