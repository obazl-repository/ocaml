

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-ocamldoc-open_lib",
    tests  = [
        ":Alias",
        ":Inner",
        ":Main",
    ],
)

ocaml_module(
    name   = "Alias",
    struct = "alias.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Inner",
    struct = "inner.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Main",
    struct = "main.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

