load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_fma_lib",
    tests  = [
        ":Fma",
    ],
)

ocaml_module(
    name   = "Fma",
    struct = "fma.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

