

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-missing-cmi-3_lib",
    tests  = [
        ":Middle",
        ":Original",
        ":User",
    ],
)

ocaml_module(
    name   = "Middle",
    struct = "middle.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Original",
    struct = "original.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "User",
    struct = "user.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

