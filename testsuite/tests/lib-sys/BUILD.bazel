## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-sys_lib",
    manifest  = [
        ":Immediate64",
        ":Opaque",
        ":Rename",
    ],
)

ocaml_module(
    name   = "Immediate64",
    struct = "immediate64.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Opaque",
    struct = "opaque.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Rename",
    struct = "rename.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

