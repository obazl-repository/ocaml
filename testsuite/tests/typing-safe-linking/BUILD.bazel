## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_typing-safe-linking_lib",
    modules  = [
        ":A",
        ":B_bad",
    ],
)

ocaml_module(
    name   = "A",
    struct = "a.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "B_bad",
    struct = "b_bad.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

