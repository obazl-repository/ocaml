

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-safe-linking_lib",
    tests  = [
        ":A",
        ":B_bad",
    ],
)

ocaml_module(
    name   = "A",
    struct = "a.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "B_bad",
    struct = "b_bad.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

