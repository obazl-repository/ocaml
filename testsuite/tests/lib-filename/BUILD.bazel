

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-filename_lib",
    tests  = [
        ":Extension",
        ":Myecho",
        ":Null",
        ":Quotecommand",
        ":Suffix",
    ],
)

ocaml_module(
    name   = "Extension",
    struct = "extension.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Myecho",
    struct = "myecho.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Null",
    struct = "null.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Quotecommand",
    struct = "quotecommand.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Suffix",
    struct = "suffix.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

