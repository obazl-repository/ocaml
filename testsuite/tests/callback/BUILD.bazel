## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_callback_lib",
    manifest  = [
        ":Signals_alloc",
        ":Tcallback",
    ],
)

ocaml_module(
    name   = "Signals_alloc",
    struct = "signals_alloc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Tcallback",
    struct = "tcallback.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

