

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_callback_lib",
    tests  = [
        ":Signals_alloc",
        ":Tcallback",
    ],
)

ocaml_module(
    name   = "Signals_alloc",
    struct = "signals_alloc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Tcallback",
    struct = "tcallback.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

