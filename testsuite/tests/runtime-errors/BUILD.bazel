

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_runtime-errors_lib",
    tests  = [
        ":Stackoverflow",
        ":Syserror",
    ],
)

ocaml_module(
    name   = "Stackoverflow",
    struct = "stackoverflow.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Syserror",
    struct = "syserror.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

