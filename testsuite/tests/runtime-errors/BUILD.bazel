## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_runtime-errors_lib",
    manifest  = [
        ":Stackoverflow",
        ":Syserror",
    ],
)

ocaml_module(
    name   = "Stackoverflow",
    struct = "stackoverflow.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Syserror",
    struct = "syserror.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

