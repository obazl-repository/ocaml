

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-hashtbl_lib",
    tests  = [
        ":Compatibility",
        ":Hfun",
        ":Htbl",
    ],
)

ocaml_module(
    name   = "Compatibility",
    struct = "compatibility.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Hfun",
    struct = "hfun.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Htbl",
    struct = "htbl.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

