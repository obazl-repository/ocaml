

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_gc-roots_lib",
    tests  = [
        ":Globroots",
    ],
)

ocaml_module(
    name   = "Globroots",
    struct = "globroots.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

