

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-int64_lib",
    tests  = [
        ":Issue9460",
        ":Test",
    ],
)

ocaml_module(
    name   = "Issue9460",
    struct = "issue9460.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

