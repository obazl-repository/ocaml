

load("//test:rules.bzl",
     "ocaml_module",
     "ocaml_signature",
)

MODULE_OPTS = []
SIG_OPTS = ["-nostdlib"]

test_suite(
    name = "testsuite_tests_basic-multdef_lib",
    tests  = [
        ":Multdef",
        ":Usemultdef",
    ],
)

ocaml_module(
    name   = "Multdef",
    struct = "multdef.ml",
    sig    = "Multdef_cmi",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_signature(
    name = "Multdef_cmi",
    src  = "multdef.mli",
    opts = SIG_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Usemultdef",
    struct = "usemultdef.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

