

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-systhreads_lib",
    tests  = [
        ":Eintr",
        ":Testfork",
        ":Testpreempt",
        ":Testyield",
        ":Threadsigmask",
    ],
)

ocaml_module(
    name   = "Eintr",
    struct = "eintr.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Testfork",
    struct = "testfork.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Testpreempt",
    struct = "testpreempt.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Testyield",
    struct = "testyield.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Threadsigmask",
    struct = "threadsigmask.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

