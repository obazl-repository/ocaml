## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_misc-unsafe_lib",
    modules  = [
        ":Almabench",
        ":Fft",
        ":Quicksort",
        ":Soli",
    ],
)

ocaml_module(
    name   = "Almabench",
    struct = "almabench.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Fft",
    struct = "fft.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Quicksort",
    struct = "quicksort.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Soli",
    struct = "soli.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

