

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-ocamlobjinfo_lib",
    tests  = [
        ":Question",
    ],
)

ocaml_module(
    name   = "Question",
    struct = "question.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

