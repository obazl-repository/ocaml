

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-missing-cmi_subdir_lib",
    tests  = [
        ":M",
    ],
)

ocaml_module(
    name   = "M",
    struct = "m.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

