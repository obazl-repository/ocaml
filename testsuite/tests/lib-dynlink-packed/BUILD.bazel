

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-dynlink-packed_lib",
    tests  = [
        ":A",
        ":B",
        ":Loader",
    ],
)

ocaml_module(
    name   = "A",
    struct = "a.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "B",
    struct = "b.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Loader",
    struct = "loader.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

