## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-dynlink-packed_lib",
    modules  = [
        ":A",
        ":B",
        ":Loader",
    ],
)

ocaml_module(
    name   = "A",
    struct = "a.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "B",
    struct = "b.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Loader",
    struct = "loader.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

