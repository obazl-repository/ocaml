

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-lexyacc_lib",
    tests  = [
        ":Calc",
    ],
)

ocaml_module(
    name   = "Calc",
    struct = "calc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

