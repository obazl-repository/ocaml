## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_tool-lexyacc_lib",
    modules  = [
        ":Calc",
    ],
)

ocaml_module(
    name   = "Calc",
    struct = "calc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

