

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-bigarray-2_lib",
    tests  = [
        ":Bigarrcml",
        ":Bigarrfml",
    ],
)

ocaml_module(
    name   = "Bigarrcml",
    struct = "bigarrcml.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Bigarrfml",
    struct = "bigarrfml.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

