## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-dynlink-pr6950_lib",
    manifest  = [
        ":B",
        ":Config",
        ":Loader",
    ],
)

ocaml_module(
    name   = "B",
    struct = "b.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Config",
    struct = "config.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Loader",
    struct = "loader.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

