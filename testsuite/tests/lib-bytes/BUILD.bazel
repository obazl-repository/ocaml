## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-bytes_lib",
    manifest  = [
        ":Binary",
        ":Test_bytes",
    ],
)

ocaml_module(
    name   = "Binary",
    struct = "binary.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test_bytes",
    struct = "test_bytes.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

