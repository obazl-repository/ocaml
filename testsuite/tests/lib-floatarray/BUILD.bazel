

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-floatarray_lib",
    tests  = [
        ":Floatarray",
    ],
)

ocaml_module(
    name   = "Floatarray",
    struct = "floatarray.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

