

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_printing-types_lib",
    tests  = [
        ":Disambiguation",
        ":Pr248",
    ],
)

ocaml_module(
    name   = "Disambiguation",
    struct = "disambiguation.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Pr248",
    struct = "pr248.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

