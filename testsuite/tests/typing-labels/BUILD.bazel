## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_typing-labels_lib",
    manifest  = [
        ":Mixin",
        ":Mixin2",
        ":Mixin3",
    ],
)

ocaml_module(
    name   = "Mixin",
    struct = "mixin.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Mixin2",
    struct = "mixin2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Mixin3",
    struct = "mixin3.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

