

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-labels_lib",
    tests  = [
        ":Mixin",
        ":Mixin2",
        ":Mixin3",
    ],
)

ocaml_module(
    name   = "Mixin",
    struct = "mixin.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Mixin2",
    struct = "mixin2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Mixin3",
    struct = "mixin3.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

