

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_ppx-attributes_lib",
    tests  = [
        ":Warning",
    ],
)

ocaml_module(
    name   = "Warning",
    struct = "warning.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

