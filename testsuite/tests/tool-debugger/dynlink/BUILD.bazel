

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-debugger_dynlink_lib",
    tests  = [
        ":Host",
        ":Plugin",
    ],
)

ocaml_module(
    name   = "Host",
    struct = "host.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Plugin",
    struct = "plugin.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

