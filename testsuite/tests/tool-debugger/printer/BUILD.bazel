## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_tool-debugger_printer_lib",
    manifest  = [
        ":Debuggee",
        ":Printer",
    ],
)

ocaml_module(
    name   = "Debuggee",
    struct = "debuggee.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Printer",
    struct = "printer.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

