

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-debugger_basic_lib",
    tests  = [
        ":Debuggee",
    ],
)

ocaml_module(
    name   = "Debuggee",
    struct = "debuggee.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

