

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-debugger_no_debug_event_lib",
    tests  = [
        ":A",
        ":B",
        ":Noev",
    ],
)

ocaml_module(
    name   = "A",
    struct = "a.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "B",
    struct = "b.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Noev",
    struct = "noev.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

