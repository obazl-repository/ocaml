## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_prim-revapply_lib",
    manifest  = [
        ":Apply",
        ":Revapply",
    ],
)

ocaml_module(
    name   = "Apply",
    struct = "apply.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Revapply",
    struct = "revapply.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

