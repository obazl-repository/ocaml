

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_prim-revapply_lib",
    tests  = [
        ":Apply",
        ":Revapply",
    ],
)

ocaml_module(
    name   = "Apply",
    struct = "apply.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Revapply",
    struct = "revapply.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

