## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lexing_lib",
    modules  = [
        ":Comments",
        ":Escape",
        ":Uchar_esc",
    ],
)

ocaml_module(
    name   = "Comments",
    struct = "comments.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Escape",
    struct = "escape.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Uchar_esc",
    struct = "uchar_esc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

