

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lexing_lib",
    tests  = [
        ":Comments",
        ":Escape",
        ":Uchar_esc",
    ],
)

ocaml_module(
    name   = "Comments",
    struct = "comments.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Escape",
    struct = "escape.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Uchar_esc",
    struct = "uchar_esc.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

