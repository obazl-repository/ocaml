## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lib-random_lib",
    manifest  = [
        ":Chi2",
        ":Full_int",
        ":Rand",
    ],
)

ocaml_module(
    name   = "Chi2",
    struct = "chi2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Full_int",
    struct = "full_int.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Rand",
    struct = "rand.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

