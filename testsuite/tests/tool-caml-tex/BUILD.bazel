

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-caml-tex_lib",
    tests  = [
        ":Ellipses",
        ":Redirections",
    ],
)

ocaml_module(
    name   = "Ellipses",
    struct = "ellipses.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Redirections",
    struct = "redirections.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

