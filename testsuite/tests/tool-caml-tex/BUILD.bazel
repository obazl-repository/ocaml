## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_tool-caml-tex_lib",
    manifest  = [
        ":Ellipses",
        ":Redirections",
    ],
)

ocaml_module(
    name   = "Ellipses",
    struct = "ellipses.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Redirections",
    struct = "redirections.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

