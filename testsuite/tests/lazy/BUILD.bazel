## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_lazy_lib",
    manifest  = [
        ":Lazy1",
    ],
)

ocaml_module(
    name   = "Lazy1",
    struct = "lazy1.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

