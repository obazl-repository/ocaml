

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lazy_lib",
    tests  = [
        ":Lazy1",
    ],
)

ocaml_module(
    name   = "Lazy1",
    struct = "lazy1.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

