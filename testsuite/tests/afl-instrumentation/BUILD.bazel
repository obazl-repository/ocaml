load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

ocaml_module(
    name   = "Afltest",
    struct = "afltest.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Harness",
    struct = "harness.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

