load("//test:rules.bzl",
     "test_module",
)

MODULE_OPTS = []

test_module(
    name   = "Afltest",
    struct = "afltest.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Harness",
    struct = "harness.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

