## GENERATED FILE ##

load("//bzl:rules.bzl",
     "ocaml_library",
     "ocaml_module",
     "ocaml_signature",
)

MODULE_OPTS = ["-nostdlib"]
SIG_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_regression_pr8769_lib",
    modules  = [
        ":Fortuna",
        ":Pr8769",
        ":Rng",
    ],
    signatures  = [
        ":Nocrypto_cmi",
    ],
)

ocaml_module(
    name   = "Fortuna",
    struct = "fortuna.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_signature(
    name = "Nocrypto_cmi",
    src  = "nocrypto.mli",
    opts = SIG_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Pr8769",
    struct = "pr8769.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Rng",
    struct = "rng.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

