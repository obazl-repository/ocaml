

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_regression_pr9326_lib",
    tests  = [
        ":Gc_set",
    ],
)

ocaml_module(
    name   = "Gc_set",
    struct = "gc_set.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

