## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_regression_pr9326_lib",
    manifest  = [
        ":Gc_set",
    ],
)

ocaml_module(
    name   = "Gc_set",
    struct = "gc_set.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

