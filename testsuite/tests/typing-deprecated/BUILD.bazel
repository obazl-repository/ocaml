

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_typing-deprecated_lib",
    tests  = [
        ":Alerts",
        ":Deprecated",
    ],
)

ocaml_module(
    name   = "Alerts",
    struct = "alerts.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Deprecated",
    struct = "deprecated.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

