

load("//test:rules.bzl",
     "test_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_generalized-open_lib",
    tests  = [
        ":Accepted_batch",
        ":Accepted_expect",
        ":Clambda_optim",
        ":Expansiveness",
        ":Funct_body",
        ":Gpr1506",
        ":Pr10048",
        ":Shadowing",
    ],
)

test_module(
    name   = "Accepted_batch",
    struct = "accepted_batch.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Accepted_expect",
    struct = "accepted_expect.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Clambda_optim",
    struct = "clambda_optim.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Expansiveness",
    struct = "expansiveness.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Funct_body",
    struct = "funct_body.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Gpr1506",
    struct = "gpr1506.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Pr10048",
    struct = "pr10048.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Shadowing",
    struct = "shadowing.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

