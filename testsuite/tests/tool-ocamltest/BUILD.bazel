

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-ocamltest_lib",
    tests  = [
        ":Norm1",
        ":Norm2",
        ":Norm3",
        ":Norm4",
    ],
)

ocaml_module(
    name   = "Norm1",
    struct = "norm1.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Norm2",
    struct = "norm2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Norm3",
    struct = "norm3.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Norm4",
    struct = "norm4.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

