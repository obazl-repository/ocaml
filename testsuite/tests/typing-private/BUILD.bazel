

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

ocaml_module(
    name   = "Invalid_private_row",
    struct = "invalid_private_row.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Private",
    struct = "private.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

