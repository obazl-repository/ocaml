## GENERATED FILE ##

load("//test:rules.bzl",
     "ocaml_library",
     "ocaml_module",
)

MODULE_OPTS = ["-nostdlib"]

ocaml_library(
    name = "testsuite_tests_typing-private_lib",
    manifest  = [
        ":Invalid_private_row",
        ":Private",
    ],
)

ocaml_module(
    name   = "Invalid_private_row",
    struct = "invalid_private_row.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Private",
    struct = "private.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

