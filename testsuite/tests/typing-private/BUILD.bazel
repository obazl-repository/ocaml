

load("//test:rules.bzl",
     "test_module",
)

MODULE_OPTS = []

test_module(
    name   = "Invalid_private_row",
    struct = "invalid_private_row.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

test_module(
    name   = "Private",
    struct = "private.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

