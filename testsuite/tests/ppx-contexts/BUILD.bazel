

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_ppx-contexts_lib",
    tests  = [
        ":Myppx",
        ":Test",
    ],
)

ocaml_module(
    name   = "Myppx",
    struct = "myppx.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

