

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_win-unicode_lib",
    tests  = [
        ":Mltest",
    ],
)

ocaml_module(
    name   = "Mltest",
    struct = "mltest.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

