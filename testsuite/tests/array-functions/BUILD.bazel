load("//test:rules.bzl",
     "batch_test_macro",
     "test_module",
)

test_suite(
    name = "array-functions",
    tests  = [
        ":Test_test",
    ],
)

batch_test_macro(
    name    = "Test_test",
    stdout_actual = "test.stdout",
    stdout_expected = "test.reference",
    test_module    = ":Test",
)

test_module(
    name   = "Test",
    struct = "test.ml",
    opts = ["-w", "+a"],
    stdlib_deps   = [
        "//stdlib:Stdlib.Array"
    ]
)

