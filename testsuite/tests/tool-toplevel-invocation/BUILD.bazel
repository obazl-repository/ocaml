

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_tool-toplevel-invocation_lib",
    tests  = [
        ":Print_args",
        ":Test",
    ],
)

ocaml_module(
    name   = "Print_args",
    struct = "print_args.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Test",
    struct = "test.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

