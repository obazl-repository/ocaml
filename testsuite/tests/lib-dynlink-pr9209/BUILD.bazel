

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_lib-dynlink-pr9209_lib",
    tests  = [
        ":Dyn",
        ":Lib",
        ":Lib2",
    ],
)

ocaml_module(
    name   = "Dyn",
    struct = "dyn.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Lib",
    struct = "lib.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

ocaml_module(
    name   = "Lib2",
    struct = "lib2.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

