

load("//test:rules.bzl",
     "ocaml_module",
)

MODULE_OPTS = []

test_suite(
    name = "testsuite_tests_functors_lib",
    tests  = [
        ":Functors",
    ],
)

ocaml_module(
    name   = "Functors",
    struct = "functors.ml",
    opts = MODULE_OPTS,
    deps   = [
    ]
)

