exports_files(["BUILD.bazel"])

## toolchains where buildhost == bootstrap

load("//toolchain/bootstrap:BUILD.bzl", "bootstrap_toolchain_adapter")

######## toolchain adapters ########

bootstrap_toolchain_adapter(
    name     = "bootstrap",
    linkmode = "dynamic"
)

